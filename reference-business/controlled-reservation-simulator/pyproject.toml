[project]
name = "controlled-reservation-simulator"
version = "0.1.0"
description = "ABIS M-203 Controlled Reservation Simulator — EXTERNAL BUSINESS SYSTEM TEST DOUBLE (MOCK-ONLY)"
requires-python = ">=3.10"
# No third-party dependencies. Python stdlib only.
